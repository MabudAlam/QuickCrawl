[project]
name = "quickcrawl-bench"
version = "0.1.0"
description = "Benchmark QuickCrawl against Firecrawl's scrape-content-dataset-v1"
requires-python = ">=3.10"
dependencies = [
    "aiohttp>=3.9.0",
    "datasets>=2.14.0",
]

[tool.uv]
package = false
